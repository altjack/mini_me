# =============================================================================
# Daily Report Frontend - Environment Variables Template
# =============================================================================
# Copia questo file come .env e configura i valori

# URL API Backend (default: http://localhost:5001/api)
VITE_API_URL=http://localhost:5001/api

# API Key per autenticazione (deve corrispondere a API_SECRET_KEY del backend)
# Lasciare vuoto in sviluppo locale se API_SECRET_KEY non è configurato
VITE_API_KEY=your-secret-api-key-here
